command -v docker || true
docker --version 2>&1 || true
sudo systemctl status docker --no-pager 2>&1 || true
sudo docker info 2>&1 | sed -n '1,80p'
groups
ls -ld "$HOME/SailfishOS" 2>/dev/null || true
df -h "$HOME"
