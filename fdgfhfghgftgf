cd sf-porter
./scripts/sf-porter.sh validate "$HOME/vayu-zips"



wip/vayu-bootstrap-baseline
4796177 Fix config path resolution for checked-out project
a56b7c0 Add read-only vayu baseline validator

mkdir -p "$HOME/vayu-metadata"

unzip -p lineage-18.1-20220510-UNOFFICIAL-vayu.zip META-INF/com/android/metadata > "$HOME/vayu-metadata/lineage-metadata.txt"
unzip -p lineage-18.1-20220510-UNOFFICIAL-vayu.zip dynamic_partitions_op_list > "$HOME/vayu-metadata/dynamic_partitions_op_list.txt"
unzip -p lineage-18.1-20220510-UNOFFICIAL-vayu.zip META-INF/com/google/android/updater-script > "$HOME/vayu-metadata/lineage-updater-script.txt"

unzip -p sailfishos-4.5.0.25-20240316-vayu-Verevka.zip META-INF/com/google/android/updater-script > "$HOME/vayu-metadata/sailfish-updater-script.txt"
unzip -p sailfishos-4.5.0.25-20240316-vayu-Verevka.zip META-INF/com/google/android/update-binary > "$HOME/vayu-metadata/sailfish-update-binary.txt"

unzip -p sfos-adaptation-vayu.1.4.zip META-INF/com/google/android/updater-script > "$HOME/vayu-metadata/adaptation-updater-script.txt"
unzip -p sfos-adaptation-vayu.1.4.zip setup.sh > "$HOME/vayu-metadata/adaptation-setup.sh"
unzip -p sfos-adaptation-vayu.1.4.zip sfos/usr/bin/droid/droid-hal-early-init.sh > "$HOME/vayu-metadata/early-init.sh"

for f in "$HOME"/vayu-metadata/*; do
  echo "===== $(basename "$f") ====="
  wc -c "$f"
  sed -n '1,220p' "$f"
  echo
done > "$HOME/vayu-metadata/report.txt"
cat "$HOME/vayu-metadata/report.txt"
